#include<bits/stdc++.h>
#include<vector>
using namespace std;

//void Display(in)


int main(){
    int n, m;
    scanf("%d %d", &n, &m);
    int t = n + m;
    int i = 0;
    while( i < n ){
        int elm = 0;
        scanf("%d", &elm);
        if( elm == -1 ){
            execute(a);
        }
        else{
            a.push_back(elm);
        }
    }

    return 0;
} 
