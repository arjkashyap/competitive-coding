#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    int t;
    cin >> t;
    while(t--){
    int m, n;
    int a[m], p[n];
    for(int i = 0; i < m; i++)
        cin >> a[i];
    for(int i = 0; i < n; i++){
        int pos;
        cin >> pos;
        p[i] = pos;
    }
    

    }

    return 0;
}
